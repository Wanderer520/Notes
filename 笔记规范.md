# 笔记规范

> 本库（数学 & 编程学习笔记）的书写约定。所有新笔记应遵循本规范，旧笔记逐步迁移对齐。
> 相关模板见 `_模板/` 文件夹（Obsidian：设置 → 模板 → 模板文件夹位置 选择 `_模板`）。

## 1. 目录结构

```
数学和编程/
├── 主页.base          # Obsidian Bases 数据库视图，勿手改
├── 笔记规范.md        # 本文件
├── _模板/             # 所有笔记模板
├── 图片存储/          # 图片统一存放（Obsidian 附件目录）
├── 数学工具箱/        # 数学课程学习笔记
│   └── <课程名>/      # 每门课一个文件夹，按章节组织
└── 编程工具箱/
    ├── 目录.md        # 编程总索引（MOC）
    ├── <技术分类>/    # 如 数组操作、链表操作、动态规划…
    │   ├── 目录.md    # 分类索引
    │   └── <知识点>.md
    └── 算法题/        # LeetCode 题解，按知识点归类
        ├── 目录.md    # 题解总索引
        └── <题目名>.md
```

## 2. 命名规则

- 笔记名：**简短、见名知义**，中文优先（与现有风格一致），如 `差值数组`、`两数相加`。
- 算法题笔记：使用 LeetCode 中文题名，如 `每日温度`、`商品折扣后的最终价格`。
- 不写序号前缀（`01-`），排序交给 `目录.md` 索引和 number-headings 插件。

## 3. 标签体系

- 使用**行内标签**，写在文件**第一行**（Properties 面板未启用，行内标签最直观）。
- 标签分两类：
  - **数据/技术类**：`#数组` `#链表` `#栈` `#队列` `#字符串` `#矩阵` `#排序` `#数学`
  - **方法/题型类**：`#单调栈` `#计数排序` `#双指针` `#暴力` `#边界判断` `#设计` `#模拟`
  - 平台标签：`#LeetCode`
- 方法类标签允许出现在对应小节标题旁（如 `## 计数排序` `#计数排序`），用于区分多种解法。

## 4. 笔记模板

| 笔记类型 | 模板 | 适用位置 |
|---|---|---|
| 算法题解 | `_模板/算法题模板.md` | `编程工具箱/算法题/` |
| 知识点 | `_模板/知识点模板.md` | `编程工具箱/<技术分类>/` |
| 课程笔记 | `_模板/课程笔记模板.md` | `数学工具箱/<课程名>/` |
| 目录索引 | `_模板/目录模板.md` | 各文件夹的 `目录.md` |

## 5. 题解笔记结构（算法题）

```markdown
#标签1 #标签2 #LeetCode
# 题干
<题目原文，含示例与提示>
# 我的题解
<自己的思路说明 + 代码>
# 官方题解
<官方思路 + 代码>
<版权声明块>
```

- 小节固定顺序：`题干` → `我的题解` → `官方题解`。
- 没有自己的解法就只写 `官方题解`；反之亦然，不写空小节。
- **官方题解必须保留版权声明块**（作者/链接/来源/著作权），转载自力扣官方。
- 代码注明语言（```java），关键步骤写中文注释。

## 6. 知识点笔记结构

```markdown
#标签
# 主要思想
<核心思路，配例子/图解>
# 代码实现
<模板代码 + 关键变量解释>
# 例题
[[关联题目]]
```

- `例题` 用 `[[双向链接]]` 关联 `算法题/` 下的题目，并注明"该题对应本知识点的哪一步"。
- 例题标题后可用块引用 `[[题目#锚点|说明]]` 指向具体解法小节。

## 7. 课程笔记结构（数学工具箱）

```markdown
#标签
# 章节标题
## 知识点 1
### 定义 / 定理 / 公式
### 推导
### 例题
## 疑问与待解决
```

- 一门课一个文件夹；一章一个笔记（或一个主题一个笔记）。
- 知识点之间、与编程工具箱之间尽量用 `[[双向链接]]` 建立联系。

## 8. 双向链接约定

- 知识点 → 例题：`[[每日温度]]`，在知识点笔记的 `例题` 小节。
- 例题 → 知识点：`[[单调栈]]`，放在题解笔记开头或 `官方题解` 前。
- 同类知识点互链：如 `[[计数排序]]` ↔ `[[差值数组]]`。

## 9. git 提交规范

- 信息用中文，格式：`<类型>: <简要说明>`
- 类型：`feat`（新增笔记）/ `fix`（修正错误）/ `docs`（规范、目录、模板）/ `refactor`（整理结构）
- 示例：`fix: 修正差值数组例题代码错误`、`feat: 新增单调栈知识点笔记`

## 10. 其他约定

- 图片统一放 `图片存储/`，粘贴时使用 Obsidian 默认附件路径。
- 不在笔记里堆砌未消化的原文——用自己的话重写，能复述才算学会。
- 旧笔记与规范不一致时，**以规范为准逐步迁移**，一次改一篇，避免大面积破坏。
